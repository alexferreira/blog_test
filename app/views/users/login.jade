extends auth

block main
  h2 Login

block auth
  form.form-horizontal(action='/users/session', method='POST')
    if (message.length)
      .fade.in.alert.alert-block.alert-error
        a.close(data-dismiss="alert", href="javascript:void(0)") x
        = message
    .control-group.offset2
      label.control-label(for='email') Email
      .controls
        input#username.input-xlarge(type='text', name='email', placeholder='')
    .control-group.offset2
      label.control-label(for='password') Password
      .controls
        input#password.input-xlarge(type='password', name='password', placeholder='')
    .control-group.offset2
      .controls
        button.btn.btn-success Entrar
        &nbsp;
        | or&nbsp;
        a.show-signup(href="/signup") Cadastre-se
